const express = require("express");
const router = express.Router();
//const Booking = require("../models/booking")

// router.get("/", ((req, res)=> {
//     //Booking.find(_id, req.user._id).then((bookings) => {
//         res.statusCode(200);
//         res.send(bookings);
//     }).catch((err) => {

//     })
// )